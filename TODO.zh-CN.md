# TODO（待办）

## P1 核心功能

- [ ] 增加存档/读档系统（角色属性、背包、装备、地图种子、当前状态）。
  - 验收标准：重启后可从文件恢复到一致状态。
- [ ] 增加一次性地图内容（宝箱、NPC 交互点、已清理标记）。
  - 验收标准：本局内状态可追踪；后续可并入存档。
- [ ] 增加完整流程集成测试（城镇 -> 战斗 -> 升级 -> Boss）。
  - 验收标准：基于固定随机种子的流程测试可稳定通过。

## P2 结构与可维护性

- [ ] 拆分 UI 子模块（`ui/map.rs`、`ui/sidebar.rs`、`ui/footer.rs`）。
  - 验收标准：`ui.rs` 仅负责组合渲染。
- [ ] 支持难度配置（easy/normal/hard）并由配置文件驱动。
  - 验收标准：无需改代码即可切换数值平衡方案。
- [x] 增加文本本地化层，统一管理所有界面文案。
  - 已完成：使用 `rust-i18n`，提供 `en` / `zh-CN` / `zh-TW` / `ja` / `ko` 词条文件。
  - 使用方式：运行前设置 `RPG_LANG=en|zh-CN|zh-TW|ja|ko`。
- [x] 增加游戏内设置页，可运行时切换语言。
  - 已完成：探索/城镇中按 `o` 进入设置页。
  - 使用方式：方向键或 `1..5` 选择语言，Enter 应用，`b`/`Esc` 返回。

## P3 玩法打磨

- [ ] 增加事件提示层（近期事件横幅/收益展示更清晰）。
- [ ] 扩展城镇交互（治疗师、旅店、简易任务钩子）。
- [ ] 增加敌人技能模式，提升战斗多样性。
